{
  "name": "deska",
  "version": "1.0.0",
  "private": true,
  "description": "Seguimiento gamificado de ofertas de empleo",
  "license": "MIT",
  "workspaces": [
    "packages/contracts",
    "apps/api",
    "apps/web"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "dev:api": "npm run start:dev --workspace @deska/api",
    "dev:web": "npm run dev --workspace @deska/web",
    "build": "npm run build --workspace @deska/contracts && npm run build --workspace @deska/api && npm run build --workspace @deska/web",
    "build:contracts": "npm run build --workspace @deska/contracts",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:unit": "npm run test:unit --workspaces --if-present",
    "test:integration": "npm run test:integration --workspaces --if-present"
  }
}
